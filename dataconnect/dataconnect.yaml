specVersion: "v1"
serviceId: "lumaos"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lumaos-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
